<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/size_40dp">

        <TextView
            android:id="@+id/recipe_number_txt"
            tools:text="1."
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/recipe_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textSize="@dimen/text_14sp"
            android:layout_marginStart="@dimen/size_6dp"
            app:layout_constraintTop_toTopOf="@id/recipe_number_txt"
            app:layout_constraintStart_toEndOf="@id/recipe_number_txt"
            tools:text="@string/test_text" />

        <TextView
            android:id="@+id/recipe_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/shape_to_use_btn"
            android:paddingTop="@dimen/size_10dp"
            android:paddingBottom="@dimen/size_10dp"
            android:paddingStart="@dimen/size_20dp"
            android:paddingEnd="@dimen/size_20dp"
            android:layout_marginTop="@dimen/size_20dp"
            android:clickable="true"
            android:focusable="true"
            android:text="@string/to_use"
            android:textAllCaps="false"
            android:textColor="@color/white"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/recipe_title" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>